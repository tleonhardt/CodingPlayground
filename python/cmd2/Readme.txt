This example makes use of the "cmd2" 3rd-party replacement for the built-in cmd module.

cmd2 is awesome in that it adds a lot of wonderful features on top of cmd.

However, at this time cmd2 isn't fully compatiable with Python 3.  So cmd2 should only be used with Python 2.7
